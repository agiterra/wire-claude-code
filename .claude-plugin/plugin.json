{
  "name": "wire",
  "description": "Wire channel plugin for Claude Code \u2014 real-time inbound messaging via The Wire message broker.",
  "version": "2.0.0",
  "keywords": [
    "wire",
    "channel",
    "agent",
    "mcp"
  ],
  "postInstall": "bun install --frozen-lockfile",
  "postUpdate": "bun install --frozen-lockfile"
}
